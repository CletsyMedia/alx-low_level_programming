#include "main.h"
/**
 * print_line - draws a straight line of (n) characters long.
 * @n: the number of underscores to print.
 */
void print_line(int n)
{
	int line;

	if (n > 0)

	for (line = 0; line < n; line++)

	_putchar('_');

	_putchar('\n');
}
