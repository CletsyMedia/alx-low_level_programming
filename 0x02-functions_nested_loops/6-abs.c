#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @abs: the int to check
 * Return: the absolute value of int
 */
int _abs(int abs)

{
	if (abs < 0)
	abs = abs * -1;
		return (abs);
}
